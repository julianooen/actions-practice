name: Workflow Dispatch
on:
  repository_dispatch:
    types: [one, two]
jobs:
  one:
    if: github.event.action == 'one'
    runs-on: ubuntu-latest
    steps:
      - name: Step One
        run: echo "This is job one"
  two:
    if: github.event.action == 'two'
    runs-on: ubuntu-latest
    steps:
      - name: Step Two
        run: echo "This is job two"



#  curl --location 'https://api.github.com/repos/julianooen/actions-practice/dispatches' \
# --header 'Accept: application/vnd.github+json' \
# --header 'Authorization: token <meu token>' \
# --header 'Content-Type: application/json' \
# --data '{
#     "event_type": "two",
    
#     "client_payload": {
#         "mensagem": "Olá do Postman",
#         "a": "eu sou o a",
#         "iniciado": "vim do evento iniciado",
#         "custom": "vim do custom",
#         "verdade": true
#     }
# }'       